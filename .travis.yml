language: node_js

node_js:
- '0.10'

install:
  - npm install
  - npm install -g gulp
  - npm install -g karma-cli
  - npm install -g jasmine-node

before_script:

script:
  - karma start --single-run
